<?xml version="1.0" encoding="utf-8"?>
<WinUIEx:SplashScreen
    x:Class="Mercatec.Application.SplashScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Local="using:Mercatec.Application"
    xmlns:WinUIEx="using:Mercatec.WinUIEx"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Image Source="ms-appx:///Images/Logo.png"
               HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="1"
               Width="200" />
        <StackPanel VerticalAlignment="Bottom">
            <TextBlock Text="{StaticResource AppName}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,25"
                       Foreground="#215A9D" FontWeight="Black"
                       Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock x:Name="Status" Text="Cargando..." HorizontalAlignment="Center" />
            <ProgressBar x:Name="Progress"
                         Minimum="0" Maximum="100"
                         HorizontalAlignment="Stretch"
                         Height="20" Margin="10,3" />
        </StackPanel>
    </Grid>
</WinUIEx:SplashScreen>
